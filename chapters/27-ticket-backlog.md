# 模擬案件バックログ（実務チケット集）

この章は、各章で学んだことを「実務のチケット」に変換して練習するためのバックログです。
シンプルメモアプリ（Compose + Hilt + REST + Room/DataStore）を前提に、**参画後に任されやすいタスク**を集めています。

---

## 前提

- [Unit 11: 模擬案件（チケット演習）（ガイド）](./11-unit11-guide.md) を読んでいる
- PR運用（目的/確認手順/スクショ/テスト）ができる

## この章でできるようになること

- チケットの受け入れ条件を満たす実装を、PRで小さく積み上げられる
- 調査→実装→回帰防止まで通す“実務の型”で練習できる
- 自分の弱点（UI/設計/テスト/ビルド）を、チケットで狙って鍛えられる

## 使い方

1. 1チケットを選ぶ（最初はSから）
2. 仕様（受け入れ条件）を満たす実装にする
3. 影響範囲と確認手順をPRに書く
4. できればテストを追加する（最低：状態遷移）

---

## チケット一覧

### S（小さめ：半日〜1日）

- **UI-01**：空状態を改善する（メモ0件時の導線、説明文、ボタン）
- **UI-02**：入力バリデーションを追加する（タイトル必須、エラー表示）
- **DATA-01**：通信失敗時のリトライ導線を追加する（ボタン、再試行）
- **DB-01**：一覧の並び替え（作成/更新/タイトル）と設定保存（DataStore）
- **TEST-01**：ViewModelの状態遷移テストを追加する（追加/削除）
- **DX-01**：ログ設計を整える（PIIを避け、失敗理由が追える）

### M（中くらい：1〜3日）

- **UI-03**：検索/フィルタを追加する（クエリ保持、空結果表示）
- **DATA-02**：認証ヘッダーをInterceptorで付与する（トークン更新方針も整理）
- **DATA-03**：指数バックオフ付きリトライを組み込む（失敗種別で分岐）
- **DB-02**：RoomのMigrationを追加する（破壊変更なし、テスト/検証手順）
- **TEST-02**：HiltでFakeを差し替えて結合テストを書く（Repository経由）
- **CI-01**：CIで `test` / `lint` / `assemble` を回す（PRで必須化）

### L（大きめ：3日〜1週間）

- **ARCH-01**：単一モジュールから `core/ui` を切り出す（段階移行）
- **ARCH-02**：`feature:memo` を切り出す（Navigation/DIの配線も含む）
- **REQ-01**：タブレット対応（一覧+詳細の2ペイン、選択状態保持）
- **REQ-02**：WorkManagerでバックアップ同期（制約/UniqueWork/リトライ）
- **REL-01**：Releaseビルドでの不具合を潰す（R8/Proguard、ログ、設定）

---

## 受け入れ条件テンプレ（PRに貼れる）

- 再現/確認手順：
- 期待結果：
- 変更内容：
- 影響範囲：
- テスト：
- リスク/フォロー：

---

## 演習

- [ ] Sチケットを2つ選び、2本のPRに分けて完了させる（確認手順/スクショ込み）
- [ ] M以上のチケットを1つ選び、さらに2本以上のPRに分割して進める
- [ ] 各チケットに回帰防止（テスト or 二重送信ガード等）を必ず1つ入れる

---

## AIに聞いてみよう

### 質問テンプレ（コピペ）

```text
【前提】
この章を学習しています（この章のコンテキストは共有済み）。

【やりたいこと】
（例：チケットを選びたい / 受け入れ条件を書きたい / 見積もりたい）

【今の状態】
- いまのレベル（できること/不安）：
- 今週の学習時間：
- 触りたい領域（UI/Network/DB/テストなど）：

【欲しい回答】
- 今のレベルに合うチケット候補（3つ）
- 各チケットの受け入れ条件（具体）
- PR単位の分割案
```

```text
【質問】
このチケット（要件）を実装するために、必要な設計とタスク分割（PR分割）を提案して。
受け入れ条件とテスト観点もセットで出して。
```

---

## 課題提出

この章には提出課題があります。

1. 上記の演習を完了する
2. GitHub で `feature/27-ticket-backlog` ブランチを作成し、PRを作成
3. [AI総合レビューツール](https://ai.studio/apps/drive/1AMqIqU4Bio4te7AWh5dly1Qzp7CesqP9?fullscreenApplet=true) でレビューを実行
4. 問題がなければ、スプレッドシートに **PR URL** と **完了日** を記入

---

## ふりかえり

- 見積もりが外れたチケットはどれ？なぜ外れた？
- PR分割は適切だった？大きすぎた/小さすぎたはどこ？
- 次に鍛えたい領域はどこ？（UI/設計/テスト/ビルド）

---

## 次の章

ここまでで完走です。次は [学習ロードマップと到達基準（6ヶ月で基礎→実務の入口）](./00-roadmap.md) に戻って、卒業要件のチェックを更新しましょう。
